export enum MessageTypes {
  TEXT = 'TEXT',
  IMAGE = 'IMAGE',
}

export enum MessagePriorities {
  HIGH = 1,
  LOW = 2,
}

export enum MessageSenders {
  USER = 'USER',
  AGENT = 'AGENT',
}

export enum Duration {
  ONE_DAY = 1,
  TWO_DAYS = 2,
  ONE_WEEK = 7,
  ONE_MONTH = 30,
  THREE_MONTHS = 90,
  ONE_YEAR = 365,
}
